name: sbi-workshop
channels:
  - pytorch
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - sbi
      - msprime
      - matplotlib
      - tskit
      - jupyter
      - ipykernel
